{
  "name": "Bloom",
  "env": {
    "MONGODB_URI": {
      "required": true
    },
    "NODE_CONFIG_DIR": {
      "required": true
    }
  },
  "addons": [
    "mongolab"
  ],
  "build": "npm run build"
}